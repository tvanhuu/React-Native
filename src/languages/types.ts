export default interface AppLanguage {
   common: {
      empty: string
      error: string
      success: string
      close: string
      ok: string
      yes: string
      no: string
   }
   error: {
      error: string
   }
}
